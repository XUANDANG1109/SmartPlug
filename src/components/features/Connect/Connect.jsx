// src/components/features/Connect/Connect.jsx
import React from 'react';
import './Connect.css';

const Connect = ({ isConnected, deviceConnected }) => {
    return (
        <div className="connection-status">
            <div className="status-item">
            </div>
            <div className="status-item">
            </div>
        </div>
    );
};

export default Connect;